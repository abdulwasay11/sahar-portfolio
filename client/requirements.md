## Packages
framer-motion | Page transitions and scroll-triggered animations for a premium feel
lucide-react | Elegant icons for services and UI elements
react-hook-form | Form state management
@hookform/resolvers | Zod validation resolver for forms

## Notes
Static images are imported directly from @assets/ per instructions.
The API for inquiries expects a POST request to /api/inquiries.
Fonts used: Cormorant Garamond (Display) and Lato (Body) for an elegant, professional salon look.
